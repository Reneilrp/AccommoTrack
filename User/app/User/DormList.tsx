import { View, Text } from "react-native";

export default function DormList() {
  return (
    <View style={{ flex: 1, justifyContent: "center", alignItems: "center" }}>
      <Text>🏘️ List of Dorms (public view)</Text>
    </View>
  );
}